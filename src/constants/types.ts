export type NavigationTypes = {
  id: number;
  name: string;
  url: string;
  isMobile?: boolean;
};

export type MasonryGalleryType = {
  id: number;
  imagePath: string;
  name: string;
};
